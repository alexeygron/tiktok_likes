package com.demo.tiktok_likes_new.view.custom;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ImageView;

import androidx.annotation.Nullable;

public class WasmScortImage extends ImageView {

    public WasmScortImage(Context context) {
        super(context);
    }

    public WasmScortImage(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public WasmScortImage(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public WasmScortImage(Context context, @Nullable AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
